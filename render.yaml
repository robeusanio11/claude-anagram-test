services:
  - type: web
    name: multiplayer-anagrams
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ROUND_DURATION
        value: 60
